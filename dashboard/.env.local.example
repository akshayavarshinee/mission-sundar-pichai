# Base URL of the ClearPort FastAPI backend (REST + SSE).
NEXT_PUBLIC_API_BASE=http://localhost:8080

# Base URL of the Arize Phoenix UI, used for deep links from trace steps.
NEXT_PUBLIC_PHOENIX_BASE=http://localhost:6006
